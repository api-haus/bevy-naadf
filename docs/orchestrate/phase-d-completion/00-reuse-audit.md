# Phase-D Completion — Reuse Audit

**Date:** 2026-05-15
**Auditor:** delegate-auditor (read-only on code).
**Repo:** `/mnt/archive4/DEV/bevy-naadf`, branch `main`, HEAD `6f55174`.
**Reference (C#):** `/mnt/archive4/DEV/NAADF` (read-only).
**Brief:** five Phase-D residuals (W1 wgpu barrier, W2 entity TAA, W3 flood-fill cap-28 test, W4 mega-module split, W5 faithfulness nits B-4 + B-5). Output path
specified by the orchestrator: `docs/orchestrate/phase-d-completion/00-reuse-audit.md`.

This audit looks for **existing code in the port** that already covers, partially
covers, or could be extended for each workstream — and surfaces in-flight
side-tracks whose scope overlaps. **No code mutated.**

---

## Audit table

| # | Candidate | Location (file:line) | What it does | reuse / extend / not applicable | One-line justification |
|---|---|---|---|---|---|
| W1.a | `naadf_gpu_producer_node` (render-graph node) | `crates/bevy_naadf/src/render/construction/mod.rs:1821-1906` | One-shot Algorithm-1 dispatch at the head of the `Core3d` chain; uses `RenderContext::command_encoder()` to get auto-inserted intra-encoder barriers. Already dispatches in production; the barrier hazard is for the **upload-skip** lever, not for the dispatch itself. | **extend** | The candidate Phase-D fix sits inside this node body — add an explicit `CommandEncoder.transition_resources(...)` (or equivalent barrier hint) and switch the renderer-bind layout strategy. The whole producer-chain wiring is already correct. |
| W1.b | `gpu_producer_skip_upload` lever | `crates/bevy_naadf/src/render/prepare.rs:203, 249, 326` | A hardcoded `false` constant with a long comment documenting why pure-upload-skip empirically fails. Existing branch arms already implement the zero-init chunks texture + zero-init blocks/voxels growable buffers for the skip path. | **extend** | When the barrier hazard is resolved, this is a single-line flip + a route to make it `ConstructionConfig.gpu_producer_skip_upload`. All consumer branches are already written; no design needed. |
| W1.c | dual-bind-group layout split | `crates/bevy_naadf/src/render/prepare.rs:17-30, 207-238`; `crates/bevy_naadf/src/render/construction/chunk_calc.rs:65-72`; `crates/bevy_naadf/src/render/construction/bounds_calc.rs:75-85`; `crates/bevy_naadf/src/render/construction/entity_update.rs:80-92` | The chunks `Rg32Uint` texture is bound to **two layouts on the same backing texture**: the renderer's `world_layout` uses `texture_3d<u32>` (read-only), the construction passes use `texture_storage_3d<rg32uint, read_write>`. This is the `15-design-c.md` §1.3 / §1.7 parallel-layout split — the design-time mechanism the user expects to make-or-break the barrier story. | **extend** | If the fix is "bind the chunks texture for renderer reads through the same storage-image layout (read-only access flag)", the existing layout-descriptor factories are the surface to touch — no new abstractions. |
| W1.d | W3 bounds-init first-frame seed (analogue) | `crates/bevy_naadf/src/render/construction/mod.rs:838-870` (gate on `!gpu_producer_has_run`); `prepare_construction` interior W3-init block | Already implements "wait for the producer to have run before initialising downstream queues" — the same ordering pattern Phase-D needs but with explicit barrier discipline at the texture (not buffer) level. | **reuse (pattern)** | Pattern is already proven for the storage-buffer hazard; W1's barrier hazard is its **texture-aliased** equivalent. The synchronisation gate (`gpu.gpu_producer_has_run = true` at `mod.rs:1899`) is already in place. |
| W2.a | `entity_instances_history` binding plumbing | `crates/bevy_naadf/src/assets/shaders/world_data.wgsl:114-124` (binding decl + doc block); `crates/bevy_naadf/src/render/construction/mod.rs:1505-1530` (allocation, placeholder vs real, gated by `entity_history_enabled`); `crates/bevy_naadf/src/render/construction/entity_update.rs:80-125` (copy_entity_history pipeline + dispatch); `crates/bevy_naadf/src/render/construction/config.rs:110-133, 167-174, 224-225` (`entity_history_enabled: bool`, default `false`); `crates/bevy_naadf/src/render/construction/entity_handler.rs:20, 61` (CPU-side per-frame history producer) | Full per-frame upload/dispatch chain plumbed and gated **OFF by default** — a 16 B placeholder occupies the binding slot when off. When the flag flips to `true`, `copy_entity_history` writes the `taa_index * 16384 + entity` slot every frame. | **extend (flip the flag, land the consumer)** | The producer half is **complete and ready**; Phase-D W2 is purely (a) flip `entity_history_enabled = true`, (b) thread the binding into the render-side bind-group layout (currently in the construction-side layout only — see W2.c below), (c) consume it in `shoot_ray` per the C# `getHitDataFromPlanes` entity branch. |
| W2.b | Port's first-hit reconstructor (consumer site) | `crates/bevy_naadf/src/assets/shaders/render_pipeline_common.wgsl:352-413` (`get_hit_data_from_planes`) | Direct port of C# `commonRenderPipeline.fxh:154-213` **MINUS** the `#ifdef ENTITIES` branch (lines `:183-203`). Header comment explicitly says: *"The `#ifdef ENTITIES` block (`:183-203`) is OMITTED (Phase B is entity-free — `09-design-b.md` §1 / §5.2), so the `entityInstancesHistory` + `taaIndex` parameters are dropped."* | **extend** | The omitted block IS the canonical Phase-D consumer. Port the 20-line entity branch verbatim from `commonRenderPipeline.fxh:183-203` and re-add the two stripped params; the surrounding logic is unchanged. **This is the canonical reference for W2; do not invent.** |
| W2.c | `world_layout` bind-group descriptor (renderer side) | `crates/bevy_naadf/src/render/pipelines.rs` (search "world_layout"); `crates/bevy_naadf/src/assets/shaders/world_data.wgsl:114-124` (`@group(0) @binding(7)`) | The shader binding is on the **construction-side** group (binding 7 in `world_data.wgsl`). The renderer's `world_layout` was extended to 8 bindings in Phase-C wave-3 — confirm whether binding 7 is included on the renderer side. | **extend (verify + extend)** | If renderer-side `world_layout` doesn't already include this binding, the Phase-D dispatch needs to add it (mechanical; mirror the construction layout entry). The existing 8-binding extension was the wave-3 work. |
| W2.d | C# entity TAA reference (CANONICAL — read-only) | `/mnt/archive4/DEV/NAADF/NAADF/Content/shaders/render/common/commonRenderPipeline.fxh:154-213` (`getHitDataFromPlanes` + entity branch `:183-203`); `/mnt/archive4/DEV/NAADF/NAADF/Content/shaders/render/rayTracing.fxh:48` (binding); `/mnt/archive4/DEV/NAADF/NAADF/Content/shaders/render/versions/base/renderSampleRefine.fx:133-292` (sample-refine entity reproj); `/mnt/archive4/DEV/NAADF/NAADF/Content/shaders/render/versions/base/renderTaaSampleReverse.fx:49, 85, 104, 181` (`ReprojectOld` entity branch); `/mnt/archive4/DEV/NAADF/NAADF/Content/shaders/render/versions/base/renderSpatialResampling.fx:356`; `/mnt/archive4/DEV/NAADF/NAADF/Content/shaders/world/data/entityUpdate.fx:5-9, 36-41` (producer side — copyEntityHistory) | The canonical entity-history consumer is **NOT just `getHitDataFromPlanes`** — it's also threaded into `renderTaaSampleReverse.fx` (the C# ReprojectOld pass) at 4 sites and into `renderSampleRefine.fx` at 7 sites. **The Phase-D scope, faithfully, touches more than `ray_tracing.wgsl::shoot_ray` alone.** | **not applicable (read-only ref)** | This is the canonical reference — every Phase-D W2 design decision should anchor here. The port currently consumes the binding **nowhere**; the brief understates the surface area (paper §3.6 = `getHitDataFromPlanes` entity branch + all the C# sites listed). |
| W3.a | Existing flood-fill tests (cap-NOT-exercised) | `crates/bevy_naadf/src/render/construction/change_handler.rs:301-390` (4 tests); the `compute_change_groups` helper at `change_handler.rs:140-220`; cap-28 implementation at `:185-189` (`if cur_distance < 28 { queue.push_back(next_pos); }`) | Four flood-fill tests: `flood_fill_single_isolated_edit`, `flood_fill_centre_edit_finds_26_neighbours`, **`flood_fill_distance_propagation_linear`** (the load-bearing W2 distance-propagation test on a 9×1×1-group world — this test's doc-comment at `:331-341` explicitly walks through (7,0,0)'s `cur_distance == 28` boundary case to explain why (8,0,0) is *added to changed_groups* but *not re-enqueued*), and `flood_fill_multiple_edits_no_double_count`. | **extend** | The 9×1×1 test **already exercises the cap-28 boundary** in its assertion logic + walkthrough comment; the gap is a *dedicated* test that pins the cap behaviour (e.g. a wider world that crosses the cap and asserts BFS halts at distance 28 even when more empty groups exist beyond). The W3 follow-up is small — a new `#[test]` reusing the `compute_change_groups` helper. **No new infrastructure needed.** |
| W3.b | C# cap-28 reference (CANONICAL) | `/mnt/archive4/DEV/NAADF/NAADF/World/Data/ChangeHandler.cs:73-174` (the BFS); `:81-110` cited explicitly in `change_handler.rs:327-341` doc-comment | The C# algorithm: per chunk-group, set `distanceFloodFill[neighbour] = curDistance + 4`, add to changedGroups, but only enqueue the neighbour when `curDistance < 28`. Identical semantics to the port. | **not applicable (read-only ref)** | C# is the canonical reference; the port is already bit-faithful (verified by the load-bearing test). The W3 followup is purely test coverage. |
| W4.a | `validate_gpu_construction` | `crates/bevy_naadf/src/render/construction/mod.rs:2256-2704` (449 lines) | Headless render-app harness — boots a `MinimalPlugins + RenderPlugin`, dispatches the W1 chunk_calc chain on a 1×1×1 deterministic fixture, byte-compares 388 bytes to the CPU `construct()` oracle. Self-contained: imports only `aadf::{cell, construct}`, `render::construction::{chunk_calc, hashing}`, `render::gpu_types::GpuConstructionParams`, `voxel::VoxelTypeId`. | **extend (move to sibling)** | A pure mechanical move to `render/construction/validation.rs` (sibling, matching the `bounds_calc.rs` / `chunk_calc.rs` / etc. convention). No private-type dependencies — every import is already `pub`. |
| W4.b | `validate_edit_mode` | `crates/bevy_naadf/src/render/construction/mod.rs:2705-2797` (93 lines) | Same harness pattern, runs `--edit-mode`'s `set_voxel` → flood-fill semantics over a `DenseVolume`; asserts changed-record counts. Self-contained. | **extend (move to sibling)** | Same as W4.a. Note: this is the helper that the Phase-C followup #1 updated to populate `WorldData::dense_voxel_types` (`mod.rs:2291-2306` per `16-impl-c-followups.md` Assumptions). Move keeps the touched site stable. |
| W4.c | `validate_entity_handler` | `crates/bevy_naadf/src/render/construction/mod.rs:2798-2864` (67 lines) | Smallest validate fn — exercises the entity dispatch + CPU handler; verifies `chunk_updates` / `entity_chunk_instances` / `entity_history` counts across frames A/B. Self-contained. | **extend (move to sibling)** | Same. **Note for the architect:** the three validate functions sum to **~609 lines, NOT the ~1500 the brief estimates.** The bulk of `mod.rs` (4510 lines) is: graph wiring (`naadf_gpu_producer_node` ~85 lines), `prepare_construction` (~1042 lines, `:779-1820`), the two `#[cfg(test)] mod tests` blocks (~860 lines combined at `:2865-3160, :3160-4019`), `tests_w4` (~490 lines at `:4019-`). Validation extraction will only cut ~600 lines, not 1500. |
| W4.d | Sibling-module naming convention | `crates/bevy_naadf/src/render/construction/{bounds_calc,change_handler,chunk_calc,config,entity_handler,entity_update,generator_model,hashing,map_copy,shader_drift_guard,world_change}.rs` (flat sibling layout); `crates/bevy_naadf/src/render/construction/mod.rs:59-69` (`pub mod` declarations) | All sub-modules are flat siblings, named by what they implement (handler / shader-side / config / helper). No nesting. | **reuse (convention)** | New module should be `validation.rs`, declared `pub mod validation;` at `mod.rs:59-69`. |
| W5.a | `expected_spans` (B-4) | `crates/bevy_naadf/src/e2e/gates.rs:709-753`; runtime gate at `crates/bevy_naadf/src/render/gi.rs:310` (`if gi.is_denoise`) | `expected_spans(6)` unconditionally lists `naadf_denoise` at `gates.rs:735, 748`. The runtime `naadf_denoise_node` only runs when `ExtractedGiConfig.settings.is_denoise == true` (default `true`; settable through `GiSettings.is_denoise` / panel). | **extend** | Make `expected_spans` config-aware: accept `is_denoise: bool` (or the full `ExtractedGiConfig`) and drop `naadf_denoise` from the batch-6 set when off. Existing `pub struct ExtractedGiConfig` (`render/extract.rs:264`) carries the flag; the gate harness already has access to it via `Res<ExtractedGiConfig>`. |
| W5.b | `FLAG_BLIT_FINAL_COLOR` dead-code | `crates/bevy_naadf/src/render/gpu_types.rs:132-138` (definition); `crates/bevy_naadf/src/assets/shaders/render_pipeline_common.wgsl:187` (WGSL mirror); doc-comments at `render/prepare.rs:544`, `render/mod.rs:201`, `assets/shaders/naadf_final.wgsl:28` reference the const **but the bit is never set on `prepare_frame_gpu` and never tested in WGSL** | Confirmed by grep: only doc-comments + the const decl + the WGSL mirror; no `flags |= FLAG_BLIT_FINAL_COLOR`, no `flags & FLAG_BLIT_FINAL_COLOR` runtime check. | **extend (delete)** | Mechanical removal — const + WGSL mirror + the four doc-comment references. Zero behavioural impact. |
| W5.c | `taa_layout` + `taa_first_hit_bind_group` dead-code | `crates/bevy_naadf/src/render/pipelines.rs:113, 379` (`taa_layout` descriptor — `BindGroupLayoutDescriptor`); `crates/bevy_naadf/src/render/taa.rs:265, 446-462` (`taa_first_hit_bind_group` field + per-frame build site in `prepare_taa`) | The `taa_layout` is a bind-group-layout descriptor that the `naadf_first_hit_pipeline` *references* only via the `taa_first_hit_bind_group` field; that bind-group is **built every frame** in `prepare_taa` but **never bound at any pipeline dispatch** (no `set_bind_group` site consumes it). Confirmed by full-tree grep for `taa_first_hit_bind_group` — only the build site at `:446-462` and the field reference at `:265, 462`. | **extend (delete)** | Remove the field, the per-frame build, and the descriptor. Cost is purely the per-frame `BindGroup` construction (small VRAM churn, ~tens of nanoseconds). Note: pipeline-layout dependencies — verify `naadf_first_hit_pipeline_descriptor`'s layout list doesn't include `taa_layout`; if it does (pipelines.rs ~379 territory), that pipeline-spec edit is the load-bearing change. |
| W5.d | `taa_sample_accum` no-op touch | `crates/bevy_naadf/src/assets/shaders/naadf_first_hit.wgsl` (grep confirmed `taa_sample_accum` is **referenced** in doc-comments and Rust prepare/extract sites but the in-shader no-op write — the original B-5 finding — is documented but the line citation in the brief is approximate; verify by reading the shader) | `gpu_types.rs:132-137` comment block + `render/extract.rs:239` comment both refer to "the `base/` first-hit no longer writes `taa_sample_accum`". The historical no-op `taa_sample_accum[0]` touch the brief mentions is in the shader, kept for "layout-stability" reasons. | **extend (delete with care)** | If the no-op touch is in `naadf_first_hit.wgsl`, removing it would let the entry point drop `taa_sample_accum` from its bind group — verify with a pipeline-layout audit. **Likely safe but needs the read-pass to confirm exact lines before mutating.** |
| W5.e | `GateState.fb_next`, `batch_needs_second_frame`, `Framebuffer::mean_pixel_delta` (B-3 debris) | `crates/bevy_naadf/src/e2e/gates.rs:387` (`fb_next` field on `GateState`); `:758` (`batch_needs_second_frame`); `crates/bevy_naadf/src/e2e/framebuffer.rs:337` (`mean_pixel_delta`); only caller is `crates/bevy_naadf/src/e2e/driver.rs:905` (`fb_next: None`) | Three symbols left over from the abandoned per-frame-delta temporal-stability gate. `fb_next` always passed `None` at the one call site; `batch_needs_second_frame` has no callers; `mean_pixel_delta` is only callable via `fb_next` which is never `Some(_)`. | **extend (delete)** | Mechanical removal — 3 symbols, the one None passing site, the field on GateState. Confirmed zero live callers (the comprehensive grep result earlier). |
| W5.f | Mechanical-offset guard pattern (B-6, related) | `crates/bevy_naadf/src/render/gpu_types.rs` (77+ `offset_of!` / `const _: () = assert!()` sites); `gpu_types.rs:837, 849` (struct-size asserts) | The Phase-C-installed pattern, now de-facto. Every Phase-D struct edit (panel + Dispatch A growth from 288 → 304 → 336) carries fresh offset guards. | **reuse (pattern)** | No action needed — the pattern is the norm. B-6 advisory is effectively closed for new structs; the brief mentions it as "partially addressed" and the W5 scope explicitly excludes it. |

---

## In-flight Phase-D-shadow track

A separate **Phase-D-shadow** orchestration is in flight on `main`. It overlaps W2 and W5 in non-trivial ways. Surfacing it explicitly so the orchestrator does not collide.

**Commits already landed on `main` (this is NOT a pending PR — these are HEAD-ancestor commits):**

- `1c35c7f feat(phase-d-shadow): multi-tap sun visibility in spatial resampling` — Dispatch A from `19-gi-reservoir-scope.md` §4. Added `GpuGiParams.sun_shadow_taps: u32` + 3 trailing pads (`gpu_types.rs:509-516`), grew `GpuGiParams` from **288 → 304 bytes**, edited `spatial_resampling.wgsl:529-583` to wrap the single sun-disk tap in `for sun_tap in 0..max(gi_params.sun_shadow_taps, 1u)`, default `4`. Backed by `20-impl-phase-d-shadow-A.md`.
- `4211910 feat(panel): raymarching quality panel (Bevy 0.19 native UI)` — Dispatch from `21-design-quality-panel.md`. **Grew `GpuGiParams` 304 → 336 bytes**, repurposed `GpuRenderParams._pad0a` → `max_ray_steps_primary` (layout-preserving), added 5 ray-step caps + `spatial_iter_count` to `GpuGiParams`. New `crates/bevy_naadf/src/panel.rs` (1293 lines). Touches: `gi_params.wgsl`, `naadf_first_hit.wgsl`, `naadf_global_illum.wgsl`, `ray_tracing.wgsl`, `render_pipeline_common.wgsl`, `spatial_resampling.wgsl`, `hud.rs`, `lib.rs`, `render/gi.rs`, `render/gpu_types.rs`, `render/prepare.rs`.
- `a602508 feat(panel): mouse drag-sliders + click toggles (Bevy 0.19 native)`, `1009c08 tune(panel): 8× slower slider drag for 0.01-level precision` — UX iterations on the panel.
- `32e8846 feat(ui): embed Roboto Regular for dev UI Unicode coverage` — font asset; no shader/GI impact.
- `3fd96fb feat(e2e): resize-blackness reproduction test (--resize-test flag)`, `1c5610c fix(taa): reallocate-all-on-resize ...`, `6de2335 fix(gi): cap indirect dispatch groups so GI bounce survives window resize`, `6f55174 docs: fold taa-resize-blackness into naadf-bevy-port, delete scratch dir` — a separate **TAA resize-blackness** track (post-Phase-D-shadow). Caps `count_invalid_data` workgroups at `MAX_INDIRECT_GROUPS = 32 768` in `sample_refine.wgsl` (touches a Phase-B file, not a Phase-D file).

**Overlap with the five W1–W5 audit workstreams:**

1. **W2 (entity TAA) ↔ panel + Dispatch A.** The panel landed promoted `GpuGiParams` from 288 to 336 bytes; Dispatch A added the `sun_shadow_taps` row at offset 288. If W2 adds **any** new uniform field (e.g. a feature flag for the entity-history consumer), it must land **after** offset 332 on a fresh 16-byte row, with `offset_of!` guards mirroring the Dispatch A + panel pattern. **W2 should NOT need a uniform field** if it just flips `ConstructionConfig.entity_history_enabled = true`, but if a runtime knob is wanted (mirroring `sun_shadow_taps`), this is the layout discipline to follow. The hazard class is real — 3× recurrence in Phase B per `12-alignment-gap.md` D-A.
2. **W5 (B-5 dead-code) ↔ panel.** The panel's `max_ray_steps_*` fields **do not** consume the legacy `MAX_RAY_STEPS_*` constants at `ray_tracing.wgsl:122-126`, but per the design doc (`21-design-quality-panel.md` §6) those consts are *retained as documentation-only references* — naga DCEs them. **Do NOT delete the `MAX_RAY_STEPS_*` consts during the W5 dead-code sweep.** They are intentionally retained anchors.
3. **W5 (B-4 expected_spans) ↔ TAA resize-blackness.** The resize-blackness fix capped `MAX_INDIRECT_GROUPS` in `sample_refine.wgsl` — same shader the `naadf_sample_refine` span covers. No `expected_spans` change needed (the span is still emitted); just noting that any audit of `expected_spans` should re-confirm `naadf_sample_refine` still fires post-cap-fix (the fix didn't drop the dispatch, just bounded the group count).
4. **W3 (cap-28 test) ↔ nothing in shadow track.** Independent.
5. **W4 (mod.rs split) ↔ nothing in shadow track.** Independent. **But:** the previous estimate of "1500 lines extractable" appears to be from the pre-Phase-C-followup state of the doc. Today's `validate_*` trio is **~609 lines**, not 1500. The orchestrator should retune the W4 expected payload before dispatching.

**No conflict** with W1 (the barrier hazard) — the shadow track touches only fragment-shader logic; the barrier sits at storage-texture binding aliasing.

---

## Borderline calls

- **W2.c (renderer `world_layout`).** Verdict listed as "extend (verify + extend)" — borderline because I did **not** open `pipelines.rs` to count the 8 entries and confirm whether `entity_instances_history` is currently bound on the renderer side. If it IS bound (the wave-3 extension brought it in), W2 becomes "extend" (flip the flag + consume in shader). If NOT, W2 needs a bind-group-layout edit (still small but adds a layout-revision risk). **Flip-trigger:** read `pipelines.rs` `world_layout` to confirm. The current `world_data.wgsl:114-124` decl is `@group(0) @binding(7)`; if `pipelines.rs::world_layout` builds 8 bindings, it includes 7 = entity-history.
- **W5.d (`taa_sample_accum` no-op touch).** Verdict "extend (delete with care)" — borderline because the brief cites the no-op in `naadf_first_hit.wgsl` but my grep showed only **documentation references** to the dead touch, not the live shader line. The deletion target may already be dead (i.e. the doc is stale and the touch is gone) OR may still be present but with a different identifier. **Flip-trigger:** open `naadf_first_hit.wgsl` and confirm whether `taa_sample_accum` is written by an entry point. If already deleted: drop W5.d from the dead-code sweep. If still present: include in the sweep but require a pipeline-layout audit before mutating.
- **W4 size estimate.** Verdict "extend (move to sibling)" — borderline on **scope**, not on direction. The ~1500-line figure in `12-alignment-gap.md` and `14-paper-gap.md` overstates what extracting the three validate fns will cut (audit measured **~609 lines** for the three combined). **Flip-trigger:** if the orchestrator wants a *meaningful* 1500-line cut from `mod.rs`, the split needs to include more than the validate fns — candidates are the two `#[cfg(test)] mod tests` blocks at `:2866-3160, :3161-4019` (combined ~1850 lines) and `tests_w4` at `:4020-` (~490 lines). The brief's framing should be honestly retuned to "extract validation (~600 lines) + optionally split the test modules to a sibling test file (~2000 lines)".
- **W1 fix shape.** Verdict on W1.a/b/c is "extend" but the **direction of the fix** is genuinely uncertain. The honest residual notes in `16-impl-c-followups.md` T1 explicitly call out two unknowns: (a) "explicit pipeline barrier between regime-1 GPU dispatch and the first render frame", (b) "a different bind-group aliasing strategy". The audit found the surface where either fix would land (W1.a–c) but **does not pick between (a) and (b)**. That is a design decision, not an audit one. **Flip-trigger:** if Phase-D W1 reads as `(a)`, the surface is `naadf_gpu_producer_node`'s `encoder` + a wgpu API call; if `(b)`, the surface is the four `*_layout_descriptor` factories that build `texture_storage_3d` + `texture_3d` views of the same texture.

---

## Reuse-vs-new recommendation per workstream

- **W1 (barrier hazard).** **Reuse the existing scaffolding (W1.a–d), pick a fix direction.** Zero new abstractions needed — `naadf_gpu_producer_node`, the `gpu_producer_skip_upload` lever, the dual-layout split, and the W3 `gpu_producer_has_run` gate are all in place. The Phase-D dispatch is a small surgical change in **at most three files** (the node body, `prepare.rs:203`, and one or more `*_layout_descriptor` factories). **No greenfield.**

- **W2 (entity TAA reprojection).** **Reuse 90 % — flip one flag + port one C# block.** The producer chain (`copy_entity_history` dispatch, the gated allocation, the CPU history producer) is **complete**. Phase-D is: (a) flip `ConstructionConfig.entity_history_enabled` default to `true`, (b) port the 20-line C# `commonRenderPipeline.fxh:183-203` entity branch into the port's `get_hit_data_from_planes` (`render_pipeline_common.wgsl:352-413`), wiring `entity_instances_history` + `taaIndex` into the function signature, (c) verify `world_layout` exposes the binding on the renderer side. **The brief's "land the consumer in `ray_tracing.wgsl::shoot_ray`" is one site — but the C# also threads the binding through `renderSampleRefine.fx` and `renderTaaSampleReverse.fx`** — surface this to the architect; the brief understates it.

- **W3 (flood-fill cap-28 test).** **Reuse the existing test infrastructure entirely.** Add one new `#[test]` in `change_handler.rs` using `compute_change_groups` on a wider world (e.g. `compute_change_groups([10, 1, 1], &[[0,0,0]])`) and assert the BFS does NOT reach beyond distance 28. The load-bearing test `flood_fill_distance_propagation_linear` already walks the cap boundary in its doc comment — the new test pins it as a separate assertion. **Trivial reuse.**

- **W4 (mod.rs split).** **Move the three validate fns to `render/construction/validation.rs`.** Sibling-module convention is established (`bounds_calc.rs`, `chunk_calc.rs`, etc.). All three fns are self-contained and re-import everything via `pub` paths. **Caveat**: this only cuts ~609 lines, not the brief's ~1500 — surface that to the architect so the W4 success criterion is honest. The bigger cut requires also splitting the `#[cfg(test)] mod tests` and `tests_w4` blocks to a sibling test file (Rust convention permits `tests.rs` siblings via `#[path]` attribute, or a `tests/` sub-dir).

- **W5 (B-4 + B-5).** **Reuse the existing extracted-config pattern + mechanical deletes.** B-4 is a one-arg signature change on `expected_spans` reading from the existing `ExtractedGiConfig`. B-5 is the five dead-code targets, each confirmed unused at audit time, each a single grep-and-delete:
  - `FLAG_BLIT_FINAL_COLOR` + its WGSL mirror — safe delete.
  - `taa_layout` + `taa_first_hit_bind_group` — safe delete, audit `naadf_first_hit_pipeline_descriptor` for layout-list mention.
  - `taa_sample_accum` no-op touch — verify existence first (see Borderline).
  - `GateState.fb_next`, `batch_needs_second_frame`, `Framebuffer::mean_pixel_delta` — safe delete.
  - **DO NOT** delete `ray_tracing.wgsl:122-126 MAX_RAY_STEPS_*` consts — they are intentionally retained per `21-design-quality-panel.md` §6 / the panel design.

---

## Top reuse recommendation

**No greenfield is justified for any of W1–W5.** Every workstream extends established scaffolding:
- W1 is a 2-3-line wgpu-API call inside an already-correct node.
- W2 is a 20-line C# port + one flag flip + (maybe) a layout-binding entry.
- W3 is one new `#[test]` using an existing helper.
- W4 is a mechanical move of three self-contained fns to a sibling module (with an honest scope-cut note to the architect — 600 lines, not 1500).
- W5 is mechanical deletion of confirmed-dead symbols + one signature-extension on `expected_spans`.

The most valuable existing piece is **W2's plumbing** — the producer chain (`copy_entity_history`, the gated allocation in `prepare_construction`, the `ConstructionConfig.entity_history_enabled` flag, the CPU history-producer in `entity_handler.rs`, and the binding-with-placeholder semantics in `world_data.wgsl`) is the largest body of "Phase-D ready, asleep" code in the tree. **The single best fit, by leverage, is W2.a + W2.b: flip the flag, port `commonRenderPipeline.fxh:183-203`, done.** That's the Phase-D residual where the user-visible payoff (TAA reprojection of moving entities, paper §3.6) lands with the smallest new code surface.
